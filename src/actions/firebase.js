export const ADD_IMG_TO_STORAGE = 'ADD_IMG_TO_STORAGE';

export const addImgToStorage = (name) => (dispatch) => {
    // dispatching an async action

    dispatch({
        type: ADD_IMG_TO_STORAGE,
        products
    });
};